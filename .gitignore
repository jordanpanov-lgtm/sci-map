# Local Claude Code / preview harness config — not part of the app
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Audio summaries — stored locally only, not pushed
assets/audio (do not load)/
